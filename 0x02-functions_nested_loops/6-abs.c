#include "holberton.h"
/**
 *_abs- Computes the absolute value of an integer.
 *@n: integer to computer
 *Return: Always 0.
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
			return (n);
}
	else
{
		return (n);
}
}
