#include "holberton.h"
/**
 *_strlen - Function that returns the length of a string
 *@s: Integer to computer
 *Description: return the long number
 *
 * Return: i
 */

int _strlen(char *s)

{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
